import { PageNotFound } from "../404/PageNotFound";

export const Dashboard = () => {
    return (
      <div className="Dashboard">
        <h2>Configuraciones en proceso...</h2>
        <PageNotFound />
      </div>
    );
};
